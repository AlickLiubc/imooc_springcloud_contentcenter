spring:
  cloud:
    nacos:
      config:
        # 指定nacos server的地址
        server-addr: localhost:8848
        file-extension: yaml
  application:
    # 服务名称尽量用-，不要用_，不要用特殊符号
    name: content-center
  profiles:
    active: dev
